# tests/guardrails/ — permanent regression suite for the 2026-08 audit
# remediation. One module per audit rule (see plan S3). Runs as the
# `guardrails` job in the existing quality-gate CI workflow.
