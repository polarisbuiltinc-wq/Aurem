// Iter 295 — vitest setup: extend Chai matchers with @testing-library/
// jest-dom for RTL. Loaded by every test file automatically via
// vitest.config.js -> test.setupFiles.
import "@testing-library/jest-dom/vitest";
