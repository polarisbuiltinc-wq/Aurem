# ROADMAP — prioritized backlog (post 2026-08-27 ORA Chat v2 P1-P5 checkpoint)

See PRD.md for original requirements/architecture, CHANGELOG.md for what's shipped.

## P0 — in-flight / founder-blocking (updated 2026-08-27, post-checkpoint decisions)
- **GUARDRAIL REMEDIATION (2026-08-28, master build plan approved by founder)** — Wave 0 (read-only re-verify) + Wave 1 (#2 path guard) DONE, see CHANGELOG.md for full detail. Current state:
  - `guard_config.path_guard.mode` = **warn** (default, Preview) — no writes are blocked yet, only logged + alerted. Founder reviews 48h of `GW_WARN_PATH` events (via `GET /admin/guardrails`), then flips to `block` via `POST /admin/guardrails/path_guard/mode`.
  - Next: Wave 2 (#8 ship-via-PR, effort M) is next up once founder reviews the Wave 1 WARN log — **waiting on founder go-ahead, do not start Wave 2 opportunistically.**
  - Wave 3 (#22 cost guard) is a **hard gate before DashScope key activation** — key activation is blocked until Wave 3 lands per founder's locked sequence.
- **DASHSCOPE_API_KEY** — founder is sending separately. Two conditions before real-model flip counts as "P1-P5 done on real model":
  a. Founder sets a $20 spend cap on the key in the DashScope console (founder-side action).
  b. Main agent runs this smoke test the moment `MOCK_LLM=false` is flipped: 2x plain chat (no tools), 3x tool-call flows (any 3 of the 6 read-only tools), 2x action propose→approve→execute (1 READ-tier tool call, 1 REVERSIBLE action), 1x rate-limit boundary (21st msg/hr → explicit cap message, no silent drop), 1x mock-vs-real shape check (delimiters, token pills). Then report cost-per-10-messages + per-session token totals from logs — this becomes the beta pricing base number. **Key discipline: env only, never in logs/fixtures/responses. Founder will rotate it when beta pricing is set.**
  c. **UPDATE (round 3)**: the founder can now ALSO paste the key via the new Admin Self-Serve LLM Settings UI (`/admin/settings` → Models & LLM → Add provider → role=chat → Test → Set active) instead of an env var — either path works, DB-active-config takes priority over env. Whichever path is used, run the same smoke test above once live.
- **Action Audit View** — SHIPPED 2026-08-27 (round 2). In-chat panel (`ora-chat-actions-btn` → `ora-action-audit-panel`) lists every proposal grouped by `proposal_id` (latest status only, not raw events) with risk-tier badge + description ("what will change"). Tested (backend unit + frontend component test), live-screenshotted with real seeded data.
- **Admin Self-Serve LLM Settings** — SHIPPED 2026-08-27 (round 3). `/admin/settings` → Models & LLM: add/edit/test/activate/delete any LLM provider config, zero code/deploy/restart. Fully tested (12 named unit tests + testing_agent E2E, zero action items). See CHANGELOG.md for full detail.
- **Visibility Kit Phase A (dogfood)** — IN PROGRESS (2026-08-28). Master spec received from founder (full text archived in LOOP-STATE.md's "PHASE 3" section). A1/A2/A3/A4 done (A1/A2/A4 pre-existing from an earlier SEO overhaul, A3 had 1 gap closed). A5 (PreferredSourceButton) built+tested. **Gate A NOT closed**: A6 (ChatGPT site verification) is founder-only/manual; A7's real 3x/engine citation protocol needs either founder manual runs or a future ChatGPT/Perplexity/Gemini-specific integration — day-0 proxy baseline captured in `marketing/kit-citations-day14.md`, real day-14 re-run due 2026-09-11. Phase B (product code) correctly not started per spec's own mandatory ordering.
- **P6 Page Inspector — ON HOLD**, prep-only so far:
  - Confirmed current DashScope Qwen-VL model via `integration_expert` (round 3): **`qwen3.7-plus`** is the current recommended vision model (legacy `qwen-vl-max` still works but is not recommended for new work). Pricing (list price, Beijing region, may vary/discount): `qwen3.7-plus` ¥2/1M input tokens + ¥8/1M output tokens; legacy `qwen-vl-max` ¥1.6/1M in + ¥4/1M out; `qwen3-vl-plus` ¥1/1M in + ¥10/1M out. Vision billing is token-based (image pixels → tokens), not a flat per-image price.
  - Updated `backend/.env`: `LLM_VISION_MODEL=qwen3.7-plus`. `LLM_VISION_BASE_URL`/`LLM_VISION_API_KEY` env plumbing (+ now also the DB-config `role="vision"` path from the new Settings UI) both resolve generically in `llm_client.py`. **Greenlight = a config/key + a build-time decision to actually call it from a new P6 endpoint — the client layer is ready but nothing invokes vision yet.**
  - Still needed before greenlight: 1 day of real-model (text) cost logs once `MOCK_LLM=false` → report cost/chat-session AND an estimated cost/screenshot-analysis (using the confirmed per-token pricing above + a rough token-count for a typical compressed screenshot). Founder greenlights or defers P6 after seeing that number.
- **P7 Morning Brief — DEFERRED to v2** per founder (overlaps with Visibility Kit's gate-C status notifications; also needs an email-provider decision). Spec: scheduled morning brief reusing existing supervise/Resend infra, 1 email + 1 bell event/day/recipient. Do not build until founder revisits.
- **GitHub connect — production incident**: Preview-side recovery-link UI aid shipped and tested; founder will re-run the connect flow post-deploy and confirm status goes active. `/callback` auto-links orphaned installs whenever a recoverable `state` string is present (even if the DB row expired) — logged `GH_CONNECT_STATE_RECOVERED`. The only non-recoverable case (state entirely missing/forged) now logs `GH_CONNECT_ORPHANED_INSTALL` with installation_id + GitHub account login + up to 10 repo names (round 3), so a real incident is greppable.

- **Commit-Boundary class fix (2026-09-05 round)** — DONE, testing_agent verified (`/app/test_reports/iteration_commit_boundary_2026_09_05.json`, 19/19 new + 30/30 fallback + 91/92 regression). New `services/actions/pending_action.py` deterministic state machine replaces the guard-based confirm_execution.py approach as the default (COMMIT_BOUNDARY_ENABLED=true, flag rollback available). See PRD.md 2026-09-05 entry for full detail.
  - **BLOCKED, needs founder action**: the real-UI + real-writable-repo terminal-state gate could not run — GitHub App auth on the only connected preview fixture (`funnel-repro`, installation 152797252) returns `github_rejected`/401. Founder needs to reinstall/reauthorize the App on a disposable drill repo (or confirm a different working fixture) before this specific gate can be re-attempted.
  - Follow-up (non-blocking): the upgrade-offer-then-confirm sibling bug from the prior round is now fixed as part of this same mechanism (real Stripe checkout on confirm, not a generic clarification) — no separate follow-up needed.
- **Core chat-flow dead-end (2026-09-03 round)** — ALL 4 ROOTS COMPLETE AND TESTED (testing_agent: 74/74 pass, `/app/test_reports/iteration_root2_3_4_grounding_escalation_gated_2026_09_03.json`). Founder's exact 3x-repro (opening hours fabrication -> orphan confirm -> "yes please" -> nothing pending) verified fixed, run 3x live. See CHANGELOG.md 2026-09-03 entries for Root 1-4 detail.
  - Follow-up (code-review flag): `services/ora_chat/grounding_check.py::extract_line_content_claims` only matches QUOTED content near "line N" — an unquoted "line 15: Open 9-5" pattern slips through. Trigger: fabrication audit round (see below).
  - Follow-up (code-review flag): `routers/chat.py` is 4,467 lines with 4 near-duplicated guard-wiring blocks across /send and /stream (Root 2 + Root 4 short-circuits). Consider extracting a shared `apply_guards_and_gates()` helper. Trigger: dedicated cleanup day.
  - Follow-up (code-review flag): `mode_routing.needs_edit_upgrade_offer`'s `account_has_pro=True` default is a safe backward-compat choice today but a footgun for a future caller that forgets to pass it explicitly. Trigger: if a new call site is added, pass it explicitly (no default reliance).
  - Next (per founder's stated order): fabrication audit — scan past chat transcripts for other fabricated-claim patterns beyond hours/content to widen Root 2's guard coverage. Trigger: founder go-ahead (explicitly deferred until Root 2/3/4 shipped, which is now).

## P0.5 — Test-suite honesty pass (2026-09-08, Wave-1 baseline triage; 2026 audit Risk #4 follow-up added individual-triage entries + SLA below) — REAL BUG SUSPECTS parked here, NOT blind-fixed

**SLA (added by 2026 audit Risk #4, founder-mandated):** any baseline entry that is batch-classified ("probably env-noise") rather than individually root-caused MUST be individually triaged within **14 days** of being added to this file, or it is auto-promoted to "must root-cause this round" — no indefinite batch-parking. Each ticket below now carries a `Root-caused:` line (date + verdict) so this is auditable at a glance.

- **RESOLVED 2026 audit Risk #3 — Secret/key detection was a TEST-FIXTURE ARTIFACT, not a live scanner regression.** Root-caused: the AWS/GCP-shaped literal fixtures in `test_iter212m55_security_scan.py::test_aws_key_detected`, `test_iter212m73_bug_hunt.py::test_aws_key_detected`/`test_gcp_key_detected`, and `test_iter212m120_vanguard_ci_ingest.py::test_ingest_persists_and_redacts` had been auto-redacted to literal `"***REDACTED_AWS_KEY***"`/`"***REDACTED_GOOGLE_KEY***"` text by GitHub push-protection (or an equivalent scrubber) at some point after the fixtures were committed — those placeholders can never match the (unchanged, correct) `AKIA[0-9A-Z]{16}` / `AIza[0-9A-Za-z\-_]{35}` regexes. Fixed by rebuilding the fake secrets from string-concatenation fragments (no single literal token is secret-shaped in the diff, so it won't be re-scrubbed). All 3 tests now pass. **Root-caused: 2026-09-08 (audit round) — test artifact, confirmed, fixed.**
- **TICKET — ORA Chat v2 mock-LLM stream not emitting `final` event (MEDIUM, could affect demo/preview UX).** `tests/test_iter2026_08_27_ora_chat_v2_p1_p5.py` (3 failures) + `tests/test_iter2026_08_28_ora_chat_v2_e2e.py` (3 failures) all show the SSE stream yielding `['state', 'error']` types only, never `'final'`, with a captured `WARNING services.ora_chat_v2.llm_client:llm_client.py:202 ora_chat_v2 llm_client stream error: APIConnectionError('Connection error.')` — i.e. even with `MOCK_LLM` intended, something in the stream path is attempting (and failing) a real network call. My 1-line read: a mock short-circuit check may be missing on this particular code path, or the test's own mock patch target is stale post-refactor (same class as the other `with_options`/stale-path fixes this round) — needs isolated repro with `MOCK_LLM=true` in this sandbox before concluding either way. **Root-caused: NOT YET — still batch-classified, now past due for individual triage (flagged >14 days old by the SLA above, promote to must-fix next round).**
- **TICKET — `_DB` test fixtures missing `loop_execution_log` attribute.** `tests/test_iter2026_08_27_p2_audit_spine.py::test_fail_ship_internal_error_logs_kind_internal_fault` and `tests/test_iter2026_08_27_show_outcome_not_engine.py::test_confirm_ship_typeerror_produces_friendly_message_with_ref_id` both log `AttributeError("'_DB' object has no attribute 'loop_execution_log'")` from `services/loop_beta.py:273`. Likely the same fixture-drift class as the `with_options` fix (fake DB missing a collection added by later production code) — low-effort fix, not yet done this round. **Root-caused: NOT YET — past due, promote next round.**
- **TICKET — code-invariant lock tests drifted (LOW, static checks not runtime bugs).** `tests/test_session4_p1_batch.py` (3 failures: `services/local_tools.py` silent-catch site at line 149 has no `logger.debug`; a dependency-audit expects 0 HIGH vulns but got 2; `yarn.lock` resolves `vite` to `5.4.21` not the expected 6.x) and `tests/test_session5_item2_orchestrator_silent_catch_lock.py` (2 failures: `orchestrator.py` silent-catch line-number lock drifted, 3 exempted lines no longer call their hook). These are source-scanning regression locks that need a maintainer pass to either fix the underlying code or re-baseline the lock's expected line numbers/versions — not urgent, but should not be silently deleted. **Root-caused: NOT YET — past due, promote next round (LOW priority, static-only).**
- **NEW TICKET (2026 audit Risk #4, HIGH — CONFIRMED REAL REGRESSION, not test-fixture drift) — Anti-Fabrication Regen safety feature is dead code in the live `/ora-chat/message` path.** `tests/test_anti_fabrication_regen_admin.py`'s `client` fixture fails with `AttributeError: <module 'routers.ora_chat'> has no attribute 'stream_call'`. Root cause traced past the mock: `routers/ora_chat.py::send_message` (the real `/message` handler) no longer calls `one_shot`/`stream_call` at all — it now delegates the entire turn to `services.ora_chat_v2.engine.run_turn` (the newer v2 engine, imported locally inside the handler). **Confirmed by direct grep: `services/ora_chat_v2/engine.py` has ZERO references to `fabrica`, `adversarial_review`, `ORA_REGEN_ON_FABRICATION`, or `grounding_check` — none of it.** This means the whole Anti-Fabrication Regen mechanism (built 2026-08-19, `ORA_REGEN_ON_FABRICATION=1` set in `.env` specifically to enable it) is currently NOT integrated into the live admin ORA chat at all — it was silently orphaned when `/message` was rewired to the v2 engine. The unit-level `adversarial_review.trigger_reason()` tests still pass (they test the pure function in isolation), which is exactly why this was invisible until this test's fixture-level failure was individually triaged instead of batch-classified as "stale mock." **Did NOT patch the test to hide this** — a passing mock here would be actively misleading. **Needs a founder decision: (a) port the grounding-check + regen-retry logic into `services/ora_chat_v2/engine.py::run_turn`, or (b) confirm this was an intentional scope-drop during the v2 migration and formally retire the feature + this test.** **Root-caused: 2026-09-08 (audit round) — REAL regression, confirmed, NOT fixed (scope decision needed first).**
- **PARTIALLY FIXED, 1 layer deeper than expected (2026 audit Risk #4) — `tests/test_aurem_p0_bugs.py` fixture non-idempotency (8 ERROR nodeids, TWO stacked root causes).** Layer 1 (FIXED): the `created_project` fixture used a FIXED `github_url` (`https://github.com/owner/test-repo`) shared across every run (only `name` was time-randomized); when `test_zzz_cleanup_delete_project` didn't run to completion on a previous run, the leftover project permanently 409s (`already_connected`) every future run — **11 stale rows had silently accumulated in the live DB** for this exact slug by the time this was triaged. Fixed by proactively listing + deleting every leftover project for that slug before creating (a single reactive delete-and-retry wasn't enough with 11 stacked leftovers). Layer 2 (FOUND, NOT FIXED — needs a follow-up, not a quick patch): once Layer 1's dedup issue was cleared, the fixture still 400s with `pat_not_supported` — **PATs were removed as a connect method entirely per a June 2026 founder directive** (`routers/cto_projects/management.py` line ~222, explicit `elif pat: await _fail(400, "pat_not_supported", ...)`), but this test file's fixture still tries to connect via the `github_token` PAT field. The fixture needs to either mock `services.github_app.verify_installation_for_repo` to simulate an App-install connection (the only remaining supported path), or the whole file should be retired if BUG1/BUG2/BUG5 already have newer App-based coverage elsewhere. **Root-caused: 2026-09-08 (audit round) — Layer 1 real bug, fixed. Layer 2 real architecture-vs-test drift, confirmed, needs a dedicated follow-up (not fixed this round — out of scope for a same-round patch).**
- **FIXED (2026 audit Risk #2 adjacent) — `test_iter366_g16_auth_hardening.py::test_login_burst_rate_limiter_exists_and_binds` was a stale string-match, not a live gap.** Asserted `check_rate_limit(f"login-ip:` but `routers/auth.py` was refactored to the async limiter (`check_rate_limit_async`) — live-confirmed the limiter still fires (6 rapid `/auth/login` calls from one IP → 429 on the 6th). Fixed the assertion string. **Root-caused: 2026-09-08 (audit round) — test-fixture drift, confirmed, fixed.**
- **Refreshed `backend/test-baseline.txt`** (2026-09-08) to today's true failing-test snapshot, replacing the 2026-08-28 snapshot (404 entries, now stale — 135 of those now pass). See CHANGELOG.md 2026-09-08 entry for the fixed/quarantined/ticketed breakdown of the 128 tests that were failing-but-not-in-the-old-baseline.

## P1 — known gaps / non-urgent housekeeping (explicitly deferred by founder, not to be picked up opportunistically)
- **Pre-existing backend test failure — now quarantined, not fixed** (`tests/test_ora_chat.py::TestSystemPromptLayering::test_default_house_rules_content_matches_spec`). 2026-08-28 (guardrail remediation C4): tagged `@pytest.mark.known_fail_audit_2026_08` and excluded from the default `pytest` run via `pytest.ini` addopts — so it can never again be silently confused with a NEW regression. Still runs on demand via `pytest -m known_fail_audit_2026_08`; still red, still not fixed (founder ruling: backlog, not fix-now).
- **Lint cleanup pass** for the 37 pre-existing ruff/oxlint errors — captured verbatim in `/app/lint-baseline.txt` (2026-08-28, guardrail remediation C4) so future CI lint gates (Wave 4/5) diff against this baseline and only fail on genuinely NEW violations. Founder explicitly said add-to-backlog, not fix-now.
- `backend/.env` has a harmless-but-confusing duplicate `LLM_MODEL` key (line 14 legacy value now unused, superseded by `DEEPSEEK_COUNCIL_MODEL`; ORA v2's own `LLM_MODEL=qwen3.8-27b` is the live one). Left alone per the "never delete .env keys" rule.
- `routers/ora_chat.py` is ~1130 lines (down from 1777 after removing legacy pipeline) — testing_agent suggested splitting into `ora_chat_v2_router.py` once P6-P9 land, to bring it back under ~700 lines. Not blocking.
- Promptfoo `qa/simulated-user/promptfooconfig.yaml` — the `1f deploy intent` case re-tag (from the P1-P7 "Run all tasks" round) was deprioritized in favor of live P6 proof and never circled back to. Still outstanding.

## P2 — future / nice-to-have
- `ora_chat_v2` catalog currently has a small, deliberately-scoped action set (create/park backlog item, toggle a single whitelisted flag, trigger digest, set funnel SLO, set whitelisted env). Expanding the catalog is a P2+ decision for the founder to make explicitly per-action (each new action needs its own risk-tier + idempotency + audit wiring).

## FUTURE LEDGER (v2 / PARKED)

**Standing rules (R1-R7):**
- R1 — Entries here are PARKED, not work items. No agent may build an F-ID without an explicit founder go-ahead to promote it off this ledger. (no-block: a parked item never blocks or pauses an active round.)
- R2 — Any NEW parked item discovered during any session gets a new F-ID appended here (all 6 fields: ID / Name / What / Why parked / Trigger / Effort) before that session's loop ends. (dev-duties: this is every agent's own responsibility, not something to ask the founder to do.)
- R3 — No renaming, removing, or silently rewording an existing F-ID's fields without a founder ruling — append a dated note instead.
- R4 — "Trigger" is the exact condition that promotes the item off PARKED — not a date, a fact (e.g. "founder review", "Wave 2 stable 2 weeks").
- R5 — This section is founder-owned backlog, not a roadmap commitment — presence here is not a promise of when/if it ships.
- R6 — Reactivation of a parked item requires a fresh spec/message from the founder at that time, not a resurrection of old assumptions — context and constraints may have changed since parking.
- R7 — Founder does a monthly review pass of this whole ledger (park/promote/drop); agents don't self-initiate that review.

**F1-F18** (F1-F15 re-forwarded verbatim by founder 2026-08-29 after the seed text was confirmed absent from disk on 2026-08-28 — used exactly as given, not reworded. F16/F17 were seeded 2026-08-28. F18 added this round.)

| ID | Name | What | Why parked | Trigger | Effort |
|---|---|---|---|---|---|
| F1 | Multi-provider LLM failover | v2 | auto-failover can mask a broken active config (no-silent-failure violation); no value with 1 provider | trigger: a 2nd LLM provider actually in active use | — |
| F2 | Cloudflare Workers AI fallback | v2 | redundancy deferred until DashScope proven | trigger: before prod launch, after ~30 days stable DashScope | — |
| F3 | Morning Brief (P7: daily email + bell) | v2 | needs email provider; overlaps kit notifications | trigger: email provider chosen + kit Gate C done | — |
| F4 | AI Share-of-Voice monitoring (paid) | v2 | highest-value paid add-on | trigger: after Kit Phase B GA + pricing validated | — |
| F5 | Kit Phase B (apply engine) | next | C7 interlock (after guardrail Waves 1+4) | trigger: founder message "kit green, start B" | — |
| F6 | Kit Phase C (Readiness score UX) | next | depends on Phase B | trigger: after F5 gate | — |
| F7 | Admin Kit & SEO Dashboard | next | kit data is file-only today, no admin surface | trigger: with F5 build OR after A7 day-14 — founder's call | — |
| F8 | Bloat audit (user-facing) | v2 | brand-IP caution (Ponytail-inspired only); needs ladder baseline | trigger: after ladder 2-week baseline report | — |
| F9 | Delete-chat undo (30s undo toast) | v2 | needs backend tombstone/soft-delete infra | trigger: when chat volume justifies (founder's call) | — |
| F10 | Ladder Items 2+3 (prompt ladder + preflight gate) | next | needs 2-week lines/ship + cost baseline | trigger: founder message "ladder green, build 2+3" after the baseline report | — |
| F11 | Personal Track official launch | v2 | founder ruling: future moat, hidden now, code intact | trigger: founder's explicit GO | — |
| F12 | ChatPanel engine migration (user chat on Qwen v2) | v2 | 6k-line engine migration = future project | trigger: after UX wave + P1 wave stable 2 weeks | — |
| F13 | Kit v2 (Article/BlogPosting schema, hreflang, CWV code fixes, internal-link/orphan, topical clusters, multilingual llms.txt) | v2 | out of v1 scope | trigger: Kit GA + demand | — |
| F14 | Ladder prompt in ORA v2 admin copilot | v2 | internal tool, separate decision | trigger: founder's call, maybe never | — |
| F15 | Housekeeping batch (404 test baseline + 37 lint) | housekeeping | baselined, untouched | trigger: dedicated cleanup day (founder's call) | — |
| F16 | Day-1 lighter repo-connect | A lower-friction repo-connect path so a fresh signup can see product value before completing external GitHub OAuth | v2 scope; found in Session 2 — fresh signups cannot connect a repo without an external OAuth popup, no manual/paste fallback exists, confirmed the #1 quit-risk in the J1 live journey | After UX wave; founder design call | M |
| F17 | 3-ship-surface component consolidation | Fold the 3 live ship surfaces (`ShipDialog` inline, `LoopLiveFeed` ShippedRow, `ShipConfirmModal`) into ONE canonical "approve a change" component + status set | Copy is already unified ("Approve the fix" everywhere per N3), but merging 3 live, independently-tested surfaces + the new Wave-2 PR flow in one pass carries real regression risk | After Wave 2 (ship-via-PR) is stable in Preview for 2 weeks | M |
| F18 | Per-user `/ora` PIN | next | per-account lockout shipped; but the PIN is still ONE shared secret (`ORA_QUICK_PIN`); per-user needs schema + migration | trigger: dedicated auth-harden wave (founder's call) | M |
| F20 | Guardrail Wave-4 | v2 | disclosure copy · CI docs-refresh gate · per-extension content validation · orphan-commit metric | trigger: founder review (added 2026-08-30, per founder's Loop-2 spec — was missing from disk, restored from founder's verbatim wording, not reworded) | M |
| F21 | Guardrail Wave-5 | v2 | tenant-isolation sweep · copy/brand lint gates · ops runbook | trigger: founder review (added 2026-08-30, per founder's Loop-2 spec — was missing from disk, restored from founder's verbatim wording, not reworded) | M |
| F24 | S2C image-to-code | parked | vibe-coding-adjacent: turn a screenshot/mock into scaffolded code | trigger: F11 vibe-track GO + VL (vision-language) quality validated (added 2026-08-30, per founder's Loop-2 spec — was missing from disk, restored from founder's verbatim wording, not reworded) | L |
| F25 | Full pre-deploy "After fix" site preview (sandboxed execution of the changed app) | v2 | dependent on sandbox/execution layer; v1 (S1-P3, shipped 2026-08-29) is the honest screenshot+affected-pages-list interim, not a fabricated fixed render | trigger: sandbox execution layer built | L |
| F29 | Cloud browser fallback for V1 deploy-verify (`verify_browser=cloud`) | v2 | V1 (2026-08-30) ships `verify_browser` as a runtime escape hatch, "local" self-host only (`services/browser_self_test.py`'s Playwright pattern) — no cloud browser provider selected, no key configured, nothing purchased. A cloud path would matter only if local hits a bot-wall or a pod compute ceiling on a real user's site — neither observed yet against the V1e local fixture | trigger: a real local-verify failure mode that's clearly infra-capacity, not app-bug (founder review before any spend) | M |

**Note on F19/F22/F23 (2026-08-30, Loop-2 T0b):** founder's Loop-2 spec asked to verify F20/F21/F22(if split)/F24 — F20, F21, F24 were confirmed MISSING from disk and restored above with the founder's own one-line specs (R3: no invented wording). **F19, F22, F23 remain absent from disk and were NOT backfilled** — founder's Loop-2 message gave no wording for F19/F23 and only a conditional ("if split") for F22 with no content; per this ledger's own R3 ("no renaming/removing/inventing wording without a founder ruling"), we do not fabricate these. Flagging the gap here for the founder to supply the missing specs, exactly as done for F19-F24 the round before.

**Note on F18 (2026 audit, appended per R3 — correction, not reword):** F18's "Why parked" undersells the actual exposure. Live-verified this round: `POST /ora-chat/pin-login` (`routers/ora_chat.py`) takes **no Authorization header at all** — it is a fully public, unauthenticated endpoint, not "any logged-in user + shared PIN." The `<PrivateRoute>` wrapper on the `/ora` frontend route only gates the React page render; it has zero bearing on the API, which anyone on the internet can call directly. Confirmed live: `curl -X POST .../ora-chat/pin-login -d '{"pin":"0000"}'` (no auth header) returns a normal `401 invalid_pin` response, not an auth-required error. **DECIDED + DONE 2026-09-08 (Decision 2):** founder ruled full per-person 2FA is overkill for a small pre-scale team, but the "anyone-on-the-internet can brute-force the PIN" vector is real and cheap to close — shipped: (1) `ORA_ALLOWED_IPS` env allowlist, checked BEFORE the PIN, 403 for non-listed IPs — left UNSET (fail-open) since the agent cannot verify the founder's real IP from the sandbox; founder sets it when ready (see test_credentials.md). (2) PIN rotated from 4 digits to a 12-char random value (`test_credentials.md`). (3) Existing 5/hour/IP+account lockout kept as-is. (4) Every successful mint now logged to Mongo `ora_pin_mint_log` (ip+identity+timestamp) — attributable, not a nameless "PIN was right." Tests: `tests/test_ora_ip_allowlist_2026_09_08.py` (3/3 passing, includes named `test_t_ora_ip_allowlist_blocks`). **DEFERRED (documented, not built):** per-person identity + full 2FA for `/ora` — this is pre-scale security, revisit before the team grows past the founder using it solo.

**Housekeeping index (2026-08-28):** the two standing regression-discipline items are tracked at their own source-of-truth files, not duplicated here — `backend/test-baseline.txt` (404 pre-existing failures/errors) and `lint-baseline.txt` (37 backend errors fixed 2026-08-28 overnight round, count now 0 — see CHANGELOG; 1 frontend suppressed via eslint-disable comment, not removed from source). F15 stays the index pointer regardless of current counts.

