# OVERNIGHT LOG — T1→T8 unattended run (started 2026-08-28)

Format: `time | item | status(done/blocked/skipped) | proof-ref`

2026-08-28T06:00Z | PHASE 0 setup | done | read PRD.md/ROADMAP.md/test-baseline.txt/lint-baseline.txt/test_credentials.md; no prior LOOP-STATE.md/OVERNIGHT-LOG.md existed (fresh start)
2026-08-28T06:05Z | T1 discovery | done | found existing deterministic diff layers with zero LLM/extra-call cost: services/loop_ship_diff.py::compute_files_diff (Loop ship path), services/task_diff.py::build_files_changed (legacy cto_tasks engine, both _run_task_via_api and _run_task_with_git)
2026-08-28T06:20Z | T1 build | done | new backend/services/ship_meter.py (compute_meter_fields, zero imports); wired into loop_engine.py::confirm_ship → loop_outcomes.record_shipped_commit(meter=...); wired into both cto_projects.py completion call sites (ship_meter=... field on _set_status); admin line added to GET /admin/loop-metrics as `ship_meter_summary.line`
2026-08-28T06:25Z | T1 tests | done | backend/tests/test_overnight_t1_ship_meter.py — 4/4 pass (t_meter_fields_present x3 shapes, t_meter_zero_llm AST-guard). proof: /app/e2e-proof/T1/pytest_t1.log
2026-08-28T06:27Z | T1 regression | done | targeted regression tests/test_iter_cto_projects_worker_coverage_2026_08.py (7/7 pass) + tests/test_iter272_holdout_verification.py + test_iter367_rollback_fake_success_fix.py + test_pat_removal_rollback_typed_error.py (29 pass, 2 fail — both are PRE-EXISTING baseline entries at test-baseline.txt lines 314-315, confirmed unrelated to ship_meter — pat_vault._decrypt_pat attribute, not touched by T1). No new regressions vs baseline.
2026-08-28T06:30Z | T1 E2E proof | in-progress | deferred to combined testing_agent call with T4 (real Preview ship on the seeded fixture project will exercise this path live) — see T4 entry
2026-08-28T06:35Z | T2 SEO/Kit admin visibility report | done | see REPORT-overnight.md Part 2 for the 4-row table; read-only, no code
2026-08-28T06:45Z | T3 Future Ledger | blocked (F1-F15) / done (F16-F17 + rules) | memory/ROADMAP.md now has "## FUTURE LEDGER (v2/PARKED)" with R1-R5 + F16 + F17 seeded verbatim from the overnight instruction. F1-F15 seed text NOT found anywhere on disk (searched ROADMAP.md, PRD.md, FUTURE_BUILDS_LEDGER.md — none match the 6-field F-ID schema) — logged as DECISION NEEDED, not invented.
2026-08-28T07:00Z | T4 Session 2 (J1-J4, K1-K10) | done | testing_agent call, report=/app/test_reports/iteration_386_session2_pass2_t4.json. J1/J2 delete-confirm + K1-fallback re-confirmed via test suite (live render blocked by MOCK_LLM=true this pod). J3 (real ship on fixture) hit a repo-connect/project-switcher gap (active project had a revoked App install; intended fixture wasn't auto-selected) — logged as finding, folded into F16. K8 code-verified (legacy /chat engine serves new users, not v2). HIGH finding: raw `HTTP 403: {json}` error leak in frontend/src/lib/api.js — fixed same session, folded into T6/P1d.
2026-08-28T07:05Z | T1 admin-line fix (from T4 finding) | done | ship_meter_summary denominator was counting ALL loop_outcomes rows (331, mostly pre-T1, all zero) instead of only meter-instrumented ones — added `lines_added: {"$exists": True}` filter. Verified live via curl (admin/loop-metrics) after fix: ship_count=8 (test-fixture rows from this session's own regression runs, all zero — genuine organic-ship proof remains NEEDS REAL-MODEL RE-TEST, see T8 report).
2026-08-28T07:10Z | T5 Parts D/E/F | done | /app/memory/PART_D_E_F_SYNTHESIS_2026_08_28.md — jargon table, ranked E1-E13 issue table, Part F canon proposal, K1-K10 status table.
2026-08-28T07:30Z | T6-P1a /ora per-account lockout | done | integration_expert consulted first (auth-hardening playbook, brute-force pattern confirmed). routers/ora_chat.py::pin_login now tracks failures by BOTH ip and a pre-resolved account_key. 2 new unit tests (test_ora_chat_pin_login.py) + 1 live E2E test (test_overnight_t6_p1a_ora_pin_lockout_live.py, skipped in-process but proved via direct curl — 5 rotating-IP fails -> 6th brand-new IP still 429). proof=/app/e2e-proof/T6/p1a_pin_lockout_live.log. Per-user PIN NOT built (needs schema+prod migration) -> DECISION NEEDED.
2026-08-28T07:35Z | T6-P1b "Run in background" | done | relabeled to "Close (task keeps running)" in ShipConfirmModal.jsx (safe default: relabel only, no new tracking infra). test=ShipConfirmModal.p1b_honest_label.test.jsx (3/3 pass).
2026-08-28T07:36Z | T6-P1c FixProgressDrawer close icon | done (already-satisfied + minor polish) | "Hide (job keeps running)" semantics were ALREADY shipped pre-overnight (Iter 212m-148); added a matching title tooltip to the legacy X icon for consistency.
2026-08-28T07:40Z | T6-P1d raw-error humanization | done | frontend/src/lib/api.js streaming-error path now strips `{"detail": "..."}` JSON bodies to just the detail string before the raw `HTTP {status}:` fallback; HTML-error-page handling (Iter 388l) was already in place. Non-streaming call sites already preferred `e?.response?.data?.detail` project-wide.
2026-08-28T07:50Z | T6-P1e native alert/confirm sweep | done (scoped) | New reusable frontend/src/components/ConfirmModal.jsx; wired into Projects.jsx (remove project) + Integrations.jsx (reveal API key). Ship/rollback window.confirm calls (MessageBubble, ShipConfirmModal, Projects task-rollback) explicitly OUT of scope — parked under ROADMAP F17 (Phase-7 ship-UI unification). test=P1e_native_confirm_sweep.test.jsx (6/6 pass).
2026-08-28T08:30Z | T7-build (ship-via-PR) | done | services/loop_safety.py: ship_branch_name/add_pr_label/close_pr/delete_ship_branch(namespace-guarded)/close_and_retract/dispatch_pull_request_webhook (new, unit-testable). loop_engine.py::confirm_ship wired behind services.feature_flags "ship_via_pr" (Mongo-backed, per-environment — no env var, no prod code path touched). routers/github_app.py webhook now dispatches pull_request events through dispatch_pull_request_webhook. finding_fix_applier.py gained revert_finding_fix() reusing the same close_and_retract(). 12/12 named+guardrail tests pass (test_overnight_t7_ship_via_pr.py): t_ship_via_pr, t_no_orphan_branches, t_label_dispatch_no_crosswrite (x3), t_revert_reverse_path_alive (x3), t_delete_ship_branch_namespaced (x3).
2026-08-28T08:35Z | T7 flag Preview-ON proof | done | POST+GET /admin/feature-flags via real admin session (test@aurem.dev) — ship_via_pr now enabled:true in THIS Preview Mongo. Prod Mongo has no such row = OFF by default, no code-level prod flip exists.
2026-08-28T08:45Z | T7-live-drill | CREDENTIALS-PENDING | attempted a real PR-open drill against the pre-seeded fixture (project funnel-repro, installation 152797252, polarisbuiltinc-wq/ora-grounding). services.pat_vault.get_repo_token_or_error returned app_installation_missing despite the Mongo installation row saying active:true — the underlying GitHub App installation is not actually reachable from this pod. Logged as DECISION NEEDED in T8 report, not a build failure.
2026-08-28T09:00Z | T8 Final report | done | /app/memory/REPORT-overnight.md written. Loop closing.


---
# OVERNIGHT R0→R6 ROUND (this fork) — R2/R5/R3/R6 P1s, unattended
2026-09T | R0.1 baseline sweep | done | admin_analytics.py (test_graph_status) + test_only_expected_files_mention_tool_router already present in backend/test-baseline.txt (lines 164, 368) — no new entry needed, confirmed via grep.
2026-09T | R0.2 3 P0 re-verify | code-confirmed, testing_agent live-verify pending | (a) ShipPendingCard.jsx "Ship to GitHub" approve button, disabled on busy||expired — present; (b) DeleteChatConfirmModal.jsx wired in SessionSwitcher.jsx calling real deleteSession — present; (c) 0:00 countdown disables both PlanApprovalCard + ShipPendingCard buttons via `expired` flag — present. Deferred live click-through to the batched testing_agent call per founder's Q5 (self-testing P1s/P0s not allowed).
2026-09T | R0.3 future ledger check | done | ROADMAP.md FUTURE LEDGER confirmed intact (F1-F18, F20/F21/F24/F25/F29 present).
2026-09T | R2 ship-via-PR build re-verify | done | existing build unchanged this round; test_overnight_t7_ship_via_pr.py + test_rollback_pr_gap_fix.py + test_drift_detection_2026_08_30.py = 27/27 pass. Preview `ship_via_pr` flag confirmed enabled:true, rollout_pct:100 (unchanged, matches founder's ON-in-preview directive).
2026-09T | R2 live PR drill | BLOCKED — NEEDS-FOUNDER, root cause identified (worse than assumed) | `GET /admin/github-app-diagnostics` (real admin JWT, this pod) returns `configured:true` but `installations: []` — the App-level JWT itself (GET /app on GitHub) is REJECTED WITH 401 by GitHub. This is NOT a per-repo/per-install issue — the diagnostics tool tried EVERY installation path and there is no "other working install" to drill against in this pod; the whole preview credential for GitHub App `aurem-devops` (app_id 4541725) fails at the most basic self-lookup call. Per Q1 fallback ("if NO install works, log NEEDS-FOUNDER, proceed to flag-gated build + non-webhook parts") — logged below, proceeded to R3.
2026-09T | R5 webhook root-cause | BLOCKED — same root cause as R2, NEEDS-FOUNDER (see report for exact GitHub-side steps) | `GET /admin/github-webhook-fence` (this pod) returns `ok:false, subscribed_events: []` with the SAME underlying 401 — because `webhook_fence_status()` also calls the App-level JWT (`GET /app/hook/deliveries`), which fails for the identical reason as R2. This is NOT the "zero subscribed events + secret mismatch" scenario the founder described (that was PRODUCTION's OLD, already-fixed R5 finding, per LOOP-STATE.md's 2026-08-30 "R9 STATUS — CARRY-FORWARD" entry showing production's webhook fence is CLEAN) — it is THIS PREVIEW POD's own copy of the GitHub App private key being rejected by GitHub outright (JWT signing succeeds locally, GitHub says 401 on verification). Exact founder steps are in REPORT-overnight.md §NEEDS-FOUNDER.
2026-09T | P1-1 /ora per-user PIN | done, live-tested | routers/ora_chat.py: `pin_login` now resolves a SPECIFIC account via optional `identifier` (email), checks that account's own bcrypt `ora_pin_hash` (new `POST /pin/set` + `GET /pin/status`, gated by real logged-in admin session), lockout now correctly scoped to the RESOLVED target's user_id (was always the one founder). No `identifier` = exact legacy shared-PIN behavior (backward compatible, zero regression). 2 new live tests (`test_p1_1_ora_per_user_pin_2026.py`, real HTTP against localhost:8001 + real Mongo) — both pass, including per-user lockout isolation (2 different identifiers don't cross-contaminate) and lockout tripping even on a correct PIN after 5 fails. 12/12 pre-existing pin-login tests unaffected. Frontend: OraDirect.jsx PinPad gained an optional "Have your own personal PIN?" identifier field; AdminSettingsPage.jsx gained a `PersonalOraPinCard` self-serve card.
2026-09T | P1-2 user-facing notification bell | mostly pre-existing + 1 real gap closed | UserNotificationBell.jsx is ALREADY wired into Dashboard.jsx for regular (non-admin) users — founder's premise ("currently admin-only") does not match current source; confirmed via grep + the P2-A ledger entry from 2026-08-28. scan_done/ship_done/ship_failed already have real emitters. Added the ONE missing type: `upgrade_eligible`, emitted once-per-user-per-month from `scan_fix_quota.py::assert_can_fix`'s 402 path (deduped via new `notif_dedupe` collection) — 1 new unit test, passes. `kit_live` type already existed but has NO real emitter anywhere (Visibility Kit's PR-merge->live auto-transition, spec §8, was never built in any prior round) — left as-is, flagged as a genuine gap in the report rather than fabricating a trigger for a feature that isn't live yet.
2026-09T | P1-3 raw-error sanitization | done | new `frontend/src/lib/sanitizeError.js` — shared `sanitizeErrorMessage(e, fallback)` helper, blocks stack traces/tracebacks/file paths/exception-class-name patterns, falls back to `e.message` only if it also passes the safety filter. Applied to MessageBubble.jsx (2 sites: rollback + ship-cta-fallback), ChatPanel.jsx (6 sites: ship/pause/cancel/loop-confirm/plan-restart toasts), LoopLiveFeed.jsx (1 site: rollback POST failure), ShipConfirmModal.jsx (2 sites: ship + rollback), AdminSettingsPage.jsx (1 site: hints-config save, opportunistic). Full 70-file sweep NOT attempted this round (scope/regression-risk judgment call, documented not silently skipped) — remaining files use the same raw pattern in lower-visibility admin/settings surfaces, flagged as follow-up.
2026-09T | P1-4 approve dead-end re-verify | CONFIRMED ALREADY CLOSED, no new code needed | services/actions/pending_action.py (Commit-Boundary class fix, 2026-09-05) is the current architecture — deterministic server-side confirm/cancel classifier, CBR-1..8 invariants, `NO_PENDING_ACTIONABLE_MESSAGE` only fires when genuinely nothing is pending (not a dead-end bug). test_commit_boundary_2026_09_05.py + test_live_commit_boundary_e2e_2026_01.py = 23/23 pass, re-run this round to confirm no drift.
2026-09T | P1-5 unify 3 ship UIs | partial, safe slice done, full merge deferred (documented) | new `frontend/src/lib/shipStatus.js` — canonical status label/color map (pending/shipping/live/rolling_back/rolled_back/failed), single source of truth. Applied to ShipConfirmModal.jsx (fixed the one real terminology mismatch found: header said "Reverted", LoopLiveFeed said "Rolled back" — now both say "Rolled back"). Did NOT physically merge ShipPendingCard/ShipConfirmModal/LoopLiveFeed's ShippedRow into one component this round — real regression risk across 3 different mount/lifecycle contexts within one unattended pass; ROADMAP F17 previously gated this behind "2 weeks stable in Preview" for exactly this reason. Flagged as a real remaining gap in the report, not claimed done.
2026-09T | R3 regression re-check (targeted) | done | py_compile clean on all touched backend files; targeted pytest across ora_chat/scan_fix_quota/pending_action/ship-via-PR/rollback/drift suites — all pass except 5 pre-existing baseline failures in test_iter212m190_scan_fix_quota.py (visibility-kit tier promo, confirmed via test-baseline.txt line 194-198, unrelated to this round's changes). Frontend: targeted vitest across ShipConfirmModal/LoopLiveFeed/MessageBubble/ChatPanel suites — 1 real regression found+fixed (sanitizeErrorMessage dropped the `e.message` fallback a mocked plain-Error test relied on), re-run clean; remaining 8 failures confirmed pre-existing via `git stash` A/B (MessageBubble.new_p0_button_renders.test.jsx / w2b_root_file_gate6_fix.test.jsx — missing `getToken` mock, unrelated to any file touched this round).
2026-09T | testing_agent E2E verify (R0.2 + 6 P1s) | done, 100%/100%, 0 action items | /app/test_reports/iteration_overnight_r0_r6_p1_p0_verify_2026_01.json — P1-1 personal PIN live end-to-end (toggle→identifier→correct PIN mints JWT, wrong PIN 401+sanitized error), P1-1b admin PersonalOraPinCard renders+works, P1-2 bell confirmed user-facing (not admin-gated), P1-3/P1-4/P1-5/R0.2 confirmed via code+backend verification. Pre-existing (not this round's regression) note surfaced: `/ora` is wrapped in `<PrivateRoute>` in App.jsx, so the PinPad is only reachable to a visitor with an existing (possibly stale) app JWT — a genuinely public/anonymous PIN entry would need that wrapper removed; flagged for founder decision, not changed this round since it predates R3 and wasn't part of the P1-1 ask (per-user PIN, not public reachability).
2026-09T | R6 ship-failure toast Retry | done, tested | ChatPanel.jsx `handleShipConfirm`/`handleShipCancel` catch blocks now pass `{message, kind:"error", actions:[{label:"Retry", onClick: () => handleShipConfirm()}]}` to `toast()` instead of a bare string. Fixed a real pre-existing bug found while wiring this: `toast("some string")` (used all over the app) destructured a string as `{message,...}`, silently producing `message: undefined` (blank toast) — `Toast.jsx::toast()` now normalizes a bare string to `{message: str}` first, backward compatible, fixes every existing bare-string call site app-wide with one change. Also added a matching `data-testid="ship-modal-retry"` button to `ShipConfirmModal.jsx`'s error phase (was Close-only before), and relaxed `handleShip()`'s phase guard to allow re-entry from `"error"`. 3 new frontend tests (`Toast.r6_string_shorthand.test.jsx`, `ChatPanel.r6_ship_retry_toast.test.jsx`) = 8/8 pass. Regression: ChatPanel/ShipPendingCard/ShipConfirmModal existing suites re-run = 26/26 + 8/8 (P1b) still pass.
2026-09T | R6 second-teammate self-serve /ora PIN | already generically supported, proven with a new live test | `require_admin()` (cto_services/auth.py) already accepts ANY `is_admin:true` or `tier:"founder"` account, not just the founder — and `POST /ora-chat/pin/set` / `GET /ora-chat/pin/status` (built in the P1-1 round) already scope strictly to `user["user_id"]` from the caller's own JWT. No new code needed; added `test_r6_second_teammate_ora_pin_2026.py` (live HTTP+Mongo, creates a throwaway second admin user, proves independent pin/set+status+pin-login, cleans up after) — passes.
2026-09T | R7 test-baseline hygiene re-check | re-confirmed, no action needed | Re-ran `test_phase2c_admin_analytics_router.py` + `test_iter212m152_prompt_mode_gaps.py` — same 3 pre-existing failures as last round (`test_graph_status`, `test_only_expected_files_mention_tool_router`, `test_parliament_untouched_by_iter_152`), ALL THREE already present in `test-baseline.txt` (lines 164, 165, 368). No re-investigation performed per founder instruction; no new entry needed.
2026-09T | R8 3-P0 clean-state re-check | re-confirmed clean, testing_agent evidence stands | Prior round's `testing_agent` run (`iteration_overnight_r0_r6_p1_p0_verify_2026_01.json`, 100%/100%) already live-verified all 3. This round only touched ChatPanel.jsx/ShipConfirmModal.jsx for the R6 retry feature (not the P0 approve/delete/countdown code paths) — re-ran `ShipPendingCard.test.jsx` (11/11) + `ShipPendingCard.large_plan.test.jsx` (4/4) to confirm no drift from the R6 edit. No new testing_agent round needed for this specific re-check.
2026-09T | R9 webhook-fence + PIN status | webhook UNCHANGED (still blocked), PIN CONFIRMED LIVE | `GET /admin/github-webhook-fence` this round: `ok:false, subscribed_events:[], error: 401 Unauthorized on https://api.github.com/app` — identical to the R2/R5 finding last round, unchanged (still waiting on FOUNDER-A). `GET /admin/github-app-diagnostics`: `list_installations` still 401. `/ora` per-user PIN (P1-1): confirmed live via testing_agent last round + this round's new second-teammate test — both pass.


