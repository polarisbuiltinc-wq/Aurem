# Investigation — "Show the Outcome, Never the Engine" — Phase 0 (M1-M7)
Date: 2026-08-27 · Preview only · read-only · 7 source files read (core/errors.py, services/github_api_writer.py [partial], services/git_identity.py, services/loop_engine.py [multiple ranges, one file], services/local_tools.py [partial], services/vanguard_verify_agent.py [partial], + grep-only passes not counted against the cap) · 1 real LLM call (~$0.001, DeepSeek, for M7) · 0 product-code changes made

## 5-LINE SUMMARY (read this first — it changes the build)
1. **M2/P0a is a SURPRISE: this specific bug already looks FIXED.** Every real call site of `commit_files()` in the current codebase (`loop_engine.py:3438-3450`, `repo_indexing.py`, `rollback_drill.py` x2, `seo/orchestrator.py`, `rollback_two_phase.py`, `finding_fix_applier.py`, `local_tools.py:892-899`) already resolves and passes real `author_name`/`author_email` via `services/git_identity.py::resolve_git_identity()`. `commit_files()` itself hard-raises `ValueError` if either is empty (`github_api_writer.py:173-177` — no silent fallback possible). Git history confirms this was a prior fix (commit `c89ea31`, "ship/commit robustness GATE 0 wiring"). **I could not reproduce "commit_files() missing 2 required positional arguments" from any current call site** — the founder's incident is either (a) already resolved by that prior commit, or (b) from a call site I haven't found. Recommend: before building anything for P0a, confirm with the founder whether this was seen on Preview AFTER `c89ea31` landed, or if it's a historical/Production-only report.
2. **M3/M6 combined finding — the REAL live mechanism is worse than "wrong label," it's "no label at all."** `core/errors.py`'s `SCHEMA_MISMATCH` catalog message is NOT "fix your profile" (it actually says "Data format issue... try again... contact support" — already fairly neutral). More importantly: `loop_engine.py`'s ship-failure path (`_fail_ship()`, line 3643) does **NOT call `classify_exception()`/`translate_error()` at all** — it takes the raw exception (`f"GitHub push failed: {e}"`) and sends it STRAIGHT to the user via `_narrate()`/`_emit()`, unfiltered. So today, ANY exception on the ship path — a real TypeError, a KeyError, anything — reaches the user's live feed as raw Python text. `core/errors.py`'s whole taxonomy (Resilience Layer Phase 1, dated 2026-08-25) appears to be built but **not yet wired into this specific failure path**.
3. **M6 — there IS a real single seam, but only for the loop/live-feed surface, not for chat/explain.** `loop_engine.py::_narrate()` (line 3916) is a genuine, already-designed single choke point — EVERY live-feed line for loop runs flows through it before `_emit()`. Its own docstring literally states the founder's exact intent ("Text rule: ≤10 words, factual, present-tense... the helper does NOT enforce this — callers must obey") — i.e. the seam exists, the enforcement doesn't. This is the cheapest possible fix location for P0c/P1's live-feed net (wrap `_narrate`, not build new plumbing). Confirmed leak found flowing through it: `"Running Vanguard security scan…"` (`loop_engine.py:2346,2351`) — a real internal codename shown raw. HOWEVER, chat/explain/advisory answers (the `content` field from `/chat/send`/`/chat/stream`) do NOT flow through `_narrate()` at all — that's a separate, un-netted pipeline (the raw LLM output returned directly). **There is no single egress point across ALL surfaces — there are (at least) two: `_narrate()` for the loop feed, and the raw chat `content` field for explain/advisory.** D2's "one egress gate" will need to be two coordinated enforcement points reusing the same strip-list/helper, not literally one function call site.
4. **M4 — severity is already code-assigned, not LLM-guessed** (good news, no est rework needed there): `vanguard_scanner.py`'s pattern catalog hardcodes severity per regex (`eval_usage`→CRITICAL, `os_system`→HIGH, etc., lines 84-98). The calibration question (is "argument injection"/`$regex` over-labeled vs the unbounded-dict OOM under-labeled) is a NEW-pattern/severity-tuning task on this EXISTING catalog, not new infrastructure — small, bounded scope.
5. **M5 — the "1 line / 2 lines brevity chip" does not appear to exist in this codebase under any name.** Exhaustive grep across `frontend/src` for "brevity", "responseLength", "verbosity", "1 line"/"2 line"/"3 line" found zero matches for a real wired control. The two near-hits are unrelated: `Projects.jsx:1914` is a CSS text-clamp comment (unrelated UI), and `AdminHouseRules.jsx:486` is placeholder EXAMPLE COPY inside a freeform admin textarea ("e.g. Answer in 3 lines or less...") — illustrating what an admin COULD type, not a real toggle. The closest REAL precedent is `ORA_PANEL_TONE`'s own history comment (`chat.py`): a prior word/line cap ("150 words / 3 lines") existed as a PROMPT INSTRUCTION and was deliberately REMOVED (Iter 212m-22) because it caused truncated, unhelpful answers. **Recommend asking the founder where they saw this chip** (a different app? a mockup? or is it a recollection of that removed instruction-based cap?) before building M5's wiring — building a UI control search for a feature that isn't there would be wasted work.

## M1 — Live progress feed emit points
- The mechanism: `loop_engine.py::_narrate(step, tone, text, ...)` → `_emit()` → SSE frame with `data.type == "narration"`. This is called from many places inside the phase pipeline.
- CONFIRMED leak, real emit site: `loop_engine.py:2346` / `2351` — `message="Running Vanguard security scan…"` / `_narrate(..., text="Running Vanguard security scan")`. "Vanguard" is an internal codename shown raw to the user.
- CONFIRMED leak, real emit site: `_fail_ship()` (`loop_engine.py:3655-3659`) narrates `f"Ship failed: {reason[:60]}"` where `reason` can be a raw exception string (see M2/M3) — this is the "raw Python error in the live feed" surface.
- **NOT CONFIRMED by direct grep** (could not find the literal phrasing in this codebase, may be dynamically constructed or may not exist as literally quoted by the founder): "`<model> thinking…`", "injected Vanguard/security skills" (found only as a code COMMENT in `skill_context_injector.py`, not obviously narrated text), "Parallel mode — N agents" (found `used_parallel_agents`/`agent_count` as tracked FIELDS in `_emit`'s log_execution call, but no narrated user-facing string located), "e2b disabled by admin" (found as a `summary` string field inside `vanguard_verify_agent.py:574`'s return dict — status UNCONFIRMED whether this specific string ever reaches `_narrate`/the user, or stays in an internal audit-only field; would need one more read to trace its caller, which I did not spend budget on this pass).
- Recommendation: P0c's regression test should assert against the CONFIRMED leaks (Vanguard name, raw exception text) first; the unconfirmed phrasings need a fresh, budgeted trace before claiming they're fixed.

## M2 — Commit/author bug (see 5-line #1 above)
Every current call site already wires `resolve_git_identity()` correctly; `commit_files()` cannot silently proceed without both args (hard `ValueError`). This looks ALREADY FIXED at the code level. Founder input needed on whether the incident postdates the fix.

## M3 — Wrong-blame mapping (see 5-line #2 above)
`core/errors.py:156-157` — ANY `TypeError`/`KeyError`/`JSONDecodeError` classifies as `SCHEMA_MISMATCH`, regardless of whether it's a genuine user-data shape issue or an internal missing-argument bug. This IS a real design gap (a missing-arg TypeError from an internal call site is NOT a "data format issue"), but it's **moot for the specific ship-failure path** right now because that path doesn't call this classifier at all (see M6) — the raw exception goes out unfiltered, which is arguably a DIFFERENT, more urgent bug than mislabeling.
Recommendation for P0b: (a) wire `_fail_ship()` (and any other bare-`_narrate`-of-an-exception call site) through `build_error_envelope()` so the SCHEMA_MISMATCH catalog message is used INSTEAD of raw exception text — this alone fixes both "raw leak" and "wrong blame" in one move; (b) separately, consider adding a narrower classification: a `TypeError` whose message matches Python's own "missing N required positional/keyword argument" shape is a strong structural signal of an INTERNAL bug (wrong call site), not user data — this could get its own `ErrorCode` (e.g. `INTERNAL_CALL_ERROR`) distinct from `SCHEMA_MISMATCH`, still classified by TYPE+STRUCTURE (not string-parsing the message) via `TypeError.__traceback__`/`inspect`-based signature checks... this needs real design, flagging as a P0b design decision, not a given.

## M4 — Scan severity (see 5-line #4 above)
Code-assigned (`vanguard_scanner.py` regex catalog), not LLM-emitted. Calibration (which findings are over/under-labeled) needs a small follow-up trace into `full_scan_orchestrator.py`'s OTHER scanners (Bug Hunt, Health, etc. — not read this pass, budget-capped) to find where the "unbounded recent_events dict" finding is emitted and what severity it currently gets, before recommending a specific change.

## M5 — Brevity chip (see 5-line #5 above)
Not found as a real feature. Needs founder clarification before any wiring work.

## M6 — Egress seam (see 5-line #3 above)
Two separate pipelines, not one:
  1. Loop live-feed → `loop_engine.py::_narrate()` (line 3916) → real single choke point, enforcement-ready, cheap to net.
  2. Chat explain/advisory → raw `content` field from `chat_send`/`chat_stream` (`routers/chat.py`) → no seam at all today; Phase 1's `PLAIN_ENGLISH_EXPLAIN_CONTRACT` is a PROMPT-layer instruction sitting in front of this un-netted pipeline (this is exactly why the 565-token result didn't hit the ~500 target — there's no mechanical backstop yet).
Scan/refusal surfaces not traced this pass (budget) — need to confirm at P1 time whether scan findings render through their own path or reuse one of the two above.

## M7 — Re-summarize cost (real measured, not estimated)
Real DeepSeek call: input = the real 565-token explain answer (from the Plain-English Contract's live proof) + a short compression instruction (~40 tokens) → output = 141 words / ~188 tokens, kept the opt-in line, did not cut mid-sentence. **Cost: negligible** (DeepSeek's per-token pricing means a ~750-token round trip is a fraction of a cent — well under any meaningful threshold). CONFIRMED safe to add as a mechanical backstop per D1.

## Founder decision needed before P0
1. **M2**: is the commit/author bug still reproducing on Preview/Production AFTER commit `c89ea31`? If yes, I need a fresh repro (project/prompt) to find the actual un-fixed call site. If no — P0a may already be DONE; the remaining P0 work is really just M3's wiring (route `_fail_ship` and similar bare-narrate-of-exception sites through `build_error_envelope()`), which is smaller than originally scoped.
2. **M5**: where was the "1 line/2 lines" brevity chip seen? (Different app, mockup, or the removed word-cap instruction from `chat.py`'s history?) This determines whether M5/the brevity-cap wiring is in scope at all.
3. Everything else (M1 confirmed leaks, M3 wiring plan, M4 catalog-tuning scope, M6 two-seam architecture, M7 cost) is ready to build on as originally planned — awaiting go-ahead to start P0a/b/c.

---

# P0 — Trust + Functional (2026-08-27, founder-approved re-scoped plan)

## Q1/Q2 answers
- **Q1 (M2/P0a redefined as proof)**: attempted a REAL live ship to the drill repo (`aurem-rollback-testbed`, `polarisbuiltinc-wq/aurem-rollback-testbed`) using the real `resolve_git_identity()` + `commit_files()` call path. **BLOCKED by environment, not code**: this Preview pod has NO GitHub App credentials configured at all (`backend/.env` has no `GITHUB_APP_ID`/`GITHUB_APP_PRIVATE_KEY`; `GITHUB_TOKEN` is present but empty). Tested `get_repo_token_or_error()` against EVERY project in `test_admin_001`'s account (36 projects, all `auth_method` types) — 100% returned `app_installation_missing`. This is a hard infra gap in this forked pod, not something fixable in code, and not something I should work around by fabricating credentials. **Could not produce the literal live-GitHub-push proof.** What I CAN confirm, from real code inspection (no mocks): every call site correctly resolves and passes real identity, `commit_files()` cannot proceed without it (hard `ValueError`), and I added a real, passing unit-level proof (`test_confirm_ship_typeerror_produces_friendly_message_with_ref_id`) that exercises the REAL `confirm_ship()` → `commit_files()` code path end-to-end (only the actual GitHub HTTP call is mocked, since no credentials exist to make it real). TJSNDHU/Aurem confirmed NOT a real customer — it's `test_admin_001`'s own test/harness project (`iter330-history-test-proj`, `iter330-harness-proj`).
- **Q2 (M5) CONFIRMED**: the "1 line"/"2 lines" the founder saw is `frontend/src/components/CollapsibleReply.jsx:69` — `{lines} line{lines === 1 ? "" : "s"}`, a collapse/expand PREVIEW LABEL (paired with a "Collapse" button at line 89), not a brevity-request control. M5/the brevity chip is DROPPED from scope, exactly as instructed. The length need itself is still covered by P1's D1 ≤500-token net.

## P0a — status: code-verified, live-push proof blocked by environment
See above. Recommend: re-attempt the literal live-GitHub proof once this Preview pod has real GitHub App credentials (or ask the founder if there's a different pod/project with working installation access) — this is an environment gate, not a code task.

## P0b — wrong-blame + raw-error-on-ship — DONE
- `core/errors.py`: added `ErrorCode.INTERNAL_CALL_ERROR`. `classify_exception()`'s `TypeError` branch now checks CPython's own deterministic "missing N required positional/keyword-only argument" wording (a narrow, documented, interpreter-generated exception to the module's "never parse str(exc)" rule — this is NOT a third-party dependency message, it's Python's own English-only, version-stable phrasing) → `INTERNAL_CALL_ERROR`; every other `TypeError`/`KeyError`/`JSONDecodeError` still classifies `SCHEMA_MISMATCH` unchanged.
- `i18n/errors_en.json`: new `INTERNAL_CALL_ERROR` entry — title "This one's on us", explicitly says it's AUREM's own bug, "nothing to do with your account, your profile, or anything you did." No "fix/update/check your profile or account" language anywhere in it.
- `services/loop_engine.py::_fail_ship()`: now accepts an optional `exc` param. When the real exception is passed (only the one true leak site, line ~3459's `GitHub push failed: {e}`), it routes through `build_error_envelope(exc)` instead of showing raw exception text — the user now gets the catalog message + a `ref_id`, never `str(exc)`. The other 5 call sites (already clean, human-written strings) are untouched — verified byte-identical via `test_static_reason_call_sites_unaffected`.
- Regressions (`backend/tests/test_iter2026_08_27_show_outcome_not_engine.py`, `TestWhoseFault` + `TestFailShipNoRawLeak`, 6 tests): missing-arg → INTERNAL not SCHEMA ✓; other TypeErrors stay SCHEMA ✓; rendered INTERNAL message contains no profile/account-blame phrasing ✓; catalog entry exists ✓; a REAL `confirm_ship()` call with a mocked `commit_files` raising a missing-arg TypeError produces a friendly message + `ref_id`, no "TypeError"/"missing"/"positional argument" in the user-facing text or the live-feed narration ✓; static call sites unaffected ✓. All pass.

## P0c — live-feed leak — DONE (for the 2 CONFIRMED leaks from M1)
- `services/loop_engine.py::_do_scan()`: `"Running Vanguard security scan…"` → `"Running security scan…"` (both the `_emit` message and the `_narrate` text) — the confirmed, real leak site.
- `services/loop_engine.py::_narrate()`: added `_strip_engine_leak_tokens()` enforcement, applied to EVERY narration text before it's emitted (defense-in-depth per M6/D2 — the seam, not per-caller filtering). Deliberately narrow list (Vanguard→security, e2b→sandbox, "disabled by admin"→"unavailable right now") — NOT a broad path-stripping regex (a narration legitimately names the user's OWN repo, e.g. "Committing N file(s) to {owner}/{repo}" — that's their data, not engine machinery, and must stay).
- Regressions (`TestNarrationLeakStrip`, 4 tests): Vanguard stripped from the helper directly ✓; the `_do_scan` call site source no longer contains "Vanguard" ✓; e2b/"disabled by admin" stripped ✓; a REAL `_narrate()` call on a live `LoopEngine` instance strips the leak before reaching `_emit()` ✓. All pass.
- **Honest scope limit**: the OTHER phrasings the founder described ("`<model> thinking…`", "injected Vanguard/security skills", "Parallel mode — N agents") were NOT confirmed as real emitted user-facing strings in this codebase (see M1) — could not fix what I couldn't locate. If the founder can point to a fresh session/screenshot where these appear, I'll trace and fix them specifically.

## Regression check
Ran the existing narration/ship/i18n/loop-engine test files alongside the 2 new test files — 4 pre-existing baseline failures found (verified via `git stash` to exist identically before this session's changes: `test_narration_text_word_budget_and_bans`, `test_pause_response_routes_skip_at_ship`, `test_loop_ship_calls_real_github_commit_files`, `test_sanitizer_strips_xml_style_tool_call_tags` — the last one is an unrelated frontend-file assertion). 0 NEW failures. 10 new tests pass.

## Founder decision needed before P1
P0a's live-push proof is blocked by this Preview pod's missing GitHub App credentials — an infra gap, not a code gap. P0b and P0c are done, tested, real (no mocks except the one unavoidable external GitHub HTTP call in the P0b test). Ready to proceed to P1 (the egress net + severity + length + Ask Advisor) on your go-ahead — flagging that P1's "before/after 655→~300" real-model proof does NOT require GitHub credentials (it's the chat/explain path only), so P1 is NOT blocked by the same infra gap.

---

# P1 — The egress net + severity + length (2026-08-27, founder-approved: "PROCEED TO P1 as planned")

## 5-LINE SUMMARY
1. **New module `services/output_guard.py`** — the mechanical GUARANTEE layer under the Plain-English prompt instruction (which is the SHAPE layer). Two functions: `strip_machinery_leak()` (regex-based, narrow named list: council jargon, "via <model> true/false", known DB collection names, framework jargon, bare file paths → all replaced with plain phrasing) and `enforce_length_cap()` (if >500 approx tokens, ONE capped DeepSeek re-summarize pass to ~300 words, keeps the opt-in line). `apply_output_guard()` combines both and returns a `ref_id` (from `core/errors.py::new_ref_id()`) whenever either net fires — the "leak-stripped" event, logged.
2. **Wired into BOTH `chat_send` and `chat_stream`**, gated on the SAME `_plain_english_active` flag (mode "A" + `explain_plain_english_v1` flag ON) already used for the prompt instruction — placed AFTER the existing mismatch/retry/fallback resolution (not before — an earlier draft placement would have netted stale content). 3 new response fields: `leak_stripped`, `length_capped`, `output_guard_ref_id`. Ship/confirm turns (any mode other than "A") never call this module — same structural guarantee as the prompt instruction.
3. **Real live proof** (test_admin_001, `ora-grounding` repo, same question as Phase 0): this run's raw model answer ran long → `length_capped: true`, `output_guard_ref_id: "ORA-f6abb9"`, final answer **226 words / ~301 tokens** (well under the 500 target) — ends with the opt-in "Want the technical details?" line intact, no sentence cut mid-way. (This run's raw model output happened not to leak a machinery token, so `leak_stripped: false` here — the leak-strip mechanism itself is proven separately below.)
4. **Leak-strip proof** (regression test, real endpoint, mocked-only-at-the-LLM-boundary): fed a deliberately leaky answer ("5-adviser council", "chairman", "via deepseek true", a real file path, `ora_council_logs`, "pydantic"/"asyncio") through the REAL `/chat/send` code path → every leaked token gone from the final response, `leak_stripped: true`, `ref_id` present. Flip-side test confirms a mutation-shaped turn's content passes through completely untouched (ship/confirm keeps file:line, always).
5. **M4 severity calibration, done and reused (not rebuilt)**: `services/full_scan_orchestrator.py::plain_severity_and_exploitability()` adds a plain 4-tier scale ("needs your attention"/"worth fixing"/"minor"/"looks fine") + a code-assigned `exploitability` tag ("internal" vs "external") — an internal-only-reachable rule_id (e.g. `lpdos_no_body_limit_deep`) is downgraded one tier on the PLAIN scale (never on the raw `severity` field used by the actual ship-block gate, so blocking behavior is unchanged); a real secret/injection rule stays top-severity regardless. Wired into `services/loop_full_scan.py::format_ship_block_reason()`'s bracketed label. Scope-limited: this recalibrates the GENERAL mechanism; the SPECIFIC customer findings the founder described (from the TJSNDHU/Aurem scan) are, per the founder's own note, a separate customer-repo fix, not this build.

## Regressions (all pass, 0 new failures elsewhere)
`backend/tests/test_iter2026_08_27_p1_egress_net.py` (7 new: leak-strip end-to-end, length-cap end-to-end, ship-turn untouched, internal-only-downgrade, external/secret-unaffected, `_normalise()` field addition, `format_ship_block_reason()` plain label). Ran alongside `test_iter212m190_full_scan_pipeline.py` (found + fixed one real regression during this work: my first `format_ship_block_reason()` rewrite dropped the existing "3 self-heal attempt(s) exhausted" wording an existing test asserted on — restored it, kept the plain-scale addition alongside it), `test_iter348_self_scan_exclusion.py`, `test_iter114_live_popup_data.py`, all P0 test files, `test_phase2c_chat_router*`, ship-robustness files — same 4 pre-existing baseline failures as before (unrelated, previously git-stash-verified), 0 new failures.

## Founder decision needed before P2
Ready to proceed to P2 (CI leak linter, Promptfoo scenarios, permanent ship-e2e, production canary) — flagging that the ship-e2e item will hit the SAME GitHub App credential gap found in P0a (this Preview pod has none), so that specific P2 sub-item will also be a code-scaffolding-only deliverable until credentials exist, same honest caveat as before.
