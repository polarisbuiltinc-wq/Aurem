# RCA — Deploy 2 HOTFIX (Iter 328) build failure

**Deployment:** launch-pad-237 (AUREM CTO) · template `fastapi_react_mongo_shadcn_base_image` · frontend_type=cloudflare
**Failed run:** 515dbd27-cf69-45aa-a239-e1616bb653b0 (redeploy)
**Failed step:** build_image → Cloud Build step #8 `frontend-build-push`
**Cloud Build id:** f49b6d02-c38b-4629-b185-ce2284c1fd1b

## Terminating error
```
[vite:esbuild] Transform failed with 1 error:
/workspace/frontend/src/components/ChatPanel.jsx:4368:16: ERROR: Unexpected "}"
```

## Root cause — SOURCE, not manifest
`frontend/src/components/ChatPanel.jsx` is corrupted by a botched edit. The component closes correctly at line 4366 (`}`), but a **duplicated, truncated fragment of the `SecurityScanDrawer` block was appended AFTER the component's closing brace**, starting mid-token. Lines 4368–4377 in the pulled source were:

```
4366:}
4367:
4368:ject.github_repo}`        <-- starts mid-token; the stray "}" at col 16 is the esbuild "Unexpected }"
4369:            : activeProject?.name
4370:        }
4371:        open={scanOpen}
4372:        onClose={() => setScanOpen(false)}
4373:      />
4374:    </div>
4375:  );
4376:}
4377:
```

This is a garbage copy of the valid block that already sits at lines 4356–4366. Note this is NOT in either of the two `setShipPending({...})` plumbing sites the hotfix intended to touch — it's stray trailing text left in the file, so the intended 2-line ShipPendingCard change may also need re-verifying once the file parses.

## Fix (verified structurally in eph pod)
Delete the appended garbage — lines **4368–4377** of `frontend/src/components/ChatPanel.jsx`. The file must end at the component close:

```jsx
        open={scanOpen}
        onClose={() => setScanOpen(false)}
      />
    </div>
  );
}
```
(i.e. the last non-empty line is the `}` that was at line 4366; file ends there.)

### Verification performed in sandbox
- Applied `sed -i '4368,4377d'` to a copy of the pulled source.
- File now ends cleanly with `... );\n}\n`; no orphan `ject.*` fragments remain.
- Brace balance and paren balance across the whole file are both 0 (were unbalanced before).
- Could NOT run esbuild/yarn build in the sandbox — the eph image is customer-apps base and has no Node runtime — so this is a structural verification (balanced + orphan-free + clean terminal close), not a full re-build. The failing token (`Unexpected "}"` at 4368:16) is directly removed by the deletion.

## Action for E1
1. Open the RCA path returned by this tool for full detail.
2. In the source environment, delete the trailing garbage (lines 4368–4377) from `frontend/src/components/ChatPanel.jsx` so the file ends at the component's closing `}`.
3. Re-run `yarn build` / the 8 vitest tests locally to confirm the frontend compiles and the intended ShipPendingCard prop-plumbing change is intact.
4. Re-trigger the deploy. Do NOT simply resume the current run — it re-pulls the still-corrupted source and will fail identically.
